<div class="table-responsive"><table class="table table-bordered table-striped"><thead><tr><th>{{ __('Deal') }}</th><th>{{ __('Products') }}</th><th>{{ __('Price') }}</th><th>{{ __('Ends') }}</th><th>{{ __('Orders') }}</th><th>{{ __('Actions') }}</th></tr></thead><tbody>@forelse($rows as $deal)<tr><td><strong>{{ $deal->name }}</strong><br><span class="badge badge-warning">{{ $deal->discount_badge }}</span></td><td>{{ $deal->dealItems->count() }}</td><td><del>{{ PriceHelper::setCurrencyPrice($deal->original_price) }}</del><br><strong class="text-success">{{ PriceHelper::setCurrencyPrice($deal->discounted_price) }}</strong></td><td>{{ optional($deal->end_date)->format('M d, Y h:i A') }}</td><td>{{ $deal->orders_count }}</td><td><a class="btn btn-info btn-sm" href="{{ route('seller.deal.edit', $deal->id) }}"><i class="fas fa-edit"></i></a><form class="d-inline" method="post" action="{{ route('seller.deal.destroy', $deal->id) }}" onsubmit="return confirm('{{ __('Delete this deal?') }}')">@csrf @method('DELETE')<button class="btn btn-danger btn-sm"><i class="fas fa-trash"></i></button></form></td></tr>@empty<tr><td colspan="6" class="text-center text-muted">{{ __('No deals found.') }}</td></tr>@endforelse</tbody></table></div>
